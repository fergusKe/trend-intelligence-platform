name: lakehouse
version: "1.0.0"
profile: lakehouse
model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
models:
  lakehouse:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: gold
      +materialized: table

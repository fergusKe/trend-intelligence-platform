# trend-intelligence-platform — 環境變數範本
#
# 用法：複製為 .env（已在 .gitignore，永不進 git），填入真值。
#   cp .env.example .env  &&  編輯 .env 填 key
#   make pipeline-secrets            # 自動讀 .env 的 YOUTUBE_API_KEY
#
# .env 內的值只會被 make pipeline-secrets 寫進 k8s Secret（youtube-api），
# 不進 git、不印在終端、不經過任何助理對話。切勿把真 key 貼到聊天或 commit。

# YouTube Data API v3 key（唯一外部憑證）。
# 產生：Google Cloud Console → APIs & Services → Credentials → Create API key，
# 並啟用 "YouTube Data API v3"。洩漏過的 key 一律作廢重發。
YOUTUBE_API_KEY=
